Bacaloria API — Phase D + ownership + profile persist
=====================================================

ملفات جديدة (لازم تتنسخ):
  lib/jsonbinAuthz.js
  lib/jsonbinMappers.js
  lib/jsonbinDb.js

ملفات معدّلة:
  api/jsonbin.js              (handler رفيع بعد التفكيك)
  src/lib/state/cloudApi.js   (actorCode + كاش بعد patch)
  src/lib/state/cloudBootstrap.js  (دمج بروفايل كامل بعد refresh)
  SECURITY.md
  docs/REFACTOR_PLAN.md
  docs/SUPABASE_INDEXES.sql

خطوات:
1) انسخ كل الملفات فوق جذر المشروع
2) نفّذ docs/SUPABASE_INDEXES.sql في Supabase مرة واحدة
3) npm run build أو deploy على Vercel
4) اختبر: لوجين، تعديل مسار bac، ربط Google، approve أدمن، signup

مفيش JWT. مفيش routes جديدة — لسه /api/jsonbin فقط.
