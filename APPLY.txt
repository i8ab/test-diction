Fix: bac path + Google link disappear after refresh
==================================================

السبب:
- قائمة الحسابات light كانت بترجع من غير bacTrack / bacGrade / bacSpecialty / avatar / authProvider
- بعد الريفريش (خصوصًا أدمن/معلم) الواجهة كانت بتقرأ النسخة الناقصة
- دمج الحسابات كان ممكن يمسح حقول موجودة لو الـ incoming فيها undefined

الملفات:
  api/jsonbin.js              (toLightAccount + mergeAccountRow)
  lib/jsonbinAuthz.js         (sanitize يحافظ على bac + social)
  src/lib/state/cloudBootstrap.js  (دمج myAccount الكامل فوق الـ light list)
  src/lib/state/cloudApi.js   (تحديث كاش الحساب بعد accountPatch)
  SECURITY.md
  docs/SUPABASE_INDEXES.sql

انسخ فوق المشروع + lib/jsonbinAuthz.js لو لسه مش موجود من باتش الـ ownership.

اختبر:
1) حدّد المسار (bac) → احفظ → ريفريش → لازم يفضل
2) اربط Google → ريفريش → لازم يفضل
3) عدّل الاسم → ريفريش → يفضل
