Frontend: persist/flush extraction + clearer save errors
========================================================

App.jsx ~2245 → ~1738 lines (with previous extracts).

New this pack:
  src/lib/hooks/useCloudPersist.js

Also includes previous FE extracts (apply all together):
  useAppShellLifecycle, useSectionEntries, refreshLock, offlineSnapshot,
  apiErrorMessage, MainView, index.css mobile pass

Behavior: same optimistic save + conflict retries.
Errors on save paths map to Arabic/English via apiErrorMessage (403/429/conflict).

Smoke-test after deploy:
  toggle studied, edit word, profile save, admin banner if staff
