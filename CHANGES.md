# التعديلات — جدول (Schedule)

نفس مسارات مشروعك بالظبط، فك الضغط فوق src/ الحالي عندك واستبدل الملفات.

## الملفات المعدّلة
- src/lib/state/schedule.js
- src/components/schedule/SchedulePage.jsx
- src/components/schedule/schedule.css
- src/lib/utils/schedulePdf.js

## 1) الوقت بقى اختياري
- البلوك مبقاش بياخد وقت افتراضي (08:00–09:00) لو ماحددتش وقت.
- في نافذة إضافة/تعديل بلوك: تشيك بوكس "حدد وقت معيّن" — لو سايبها فاضية، البلوك بيتسجل من غير بداية/نهاية.
- الترتيب: البلوكات اللي ليها وقت بتتظهر أول بترتيب الوقت، وبعدين اللي من غير وقت بترتيب الاسم.
- تعارض الأوقات (Conflict) بيتفحص بس للبلوكات اللي ليها وقت.
- في PDF التصدير وتصدير النص: البلوك من غير وقت بيظهر "بلا وقت" بدل وقت وهمي.

## 2) مهام التكرار المتباعد (Spaced Repetition) تتضاف تلقائي
- أي مهمة من "إنجاز اليوم" (dayAchievements) عليها تكرار متباعد (SRS) ومستحقة في يوم معيّن، بتظهر تلقائي في جدول ذلك اليوم (تبويب "اليوم" و"الأسبوع") بعلامة "تكرار متباعد".
- دي عرض تلقائي بس (مش بتتحفظ داخل بيانات الجدول نفسها) — عشان تفضل محدّثة أوتوماتيك مع أي تغيير في مواعيد المراجعة، من غير تكرار بيانات.
- مينفعش تتعدل أو تتمسح من صفحة الجدول (مراجعتها الحقيقية من صفحة إنجازات اليوم)، بس تقدر تعلّمها "تم" كتذكير شخصي بس.

## 3) استيراد/تصدير الجدول
- في إعدادات الجدول (⚙️) في كارت جديد "استيراد وتصدير الجدول":
  - "تصدير ملف": بينزّل جدولك الحالي ملف JSON (my-schedule.json).
  - "استيراد ملف": تختار ملف JSON (من نفس الصيغة) فيستبدل جدولك الحالي بيه (بعد تأكيد).

## 4) قسم الأسبوع في الإعدادات (باجز)
راجعت الكود بالكامل (الـ state وربط الأزرار) وماقدرتش ألاقي خطأ برمجي واضح يسبب كراش وقت اختيار "بداية الأسبوع" أو تبويب "الأسبوع" — كله سليم منطقيًا وبيتفحص بنجاح (syntax + bundle check بـ esbuild من غير أي أخطاء).
لو لسه بيحصل باجز بعد التحديث ده، ابعتلي:
- نص الخطأ من console المتصفح (F12 → Console)، أو
- سكرين شوت للحظة اللي بيحصل فيها الباجز بالظبط
وهعدّل بالظبط على السبب الحقيقي بدل تخمين.

## 5) الـ PDF — الساعات كانت بتطبع ناقصة
السبب: `.day { overflow: hidden }` كان بيقصّ أي بلوكات زيادة عن ارتفاع الكارت في الطباعة بصمت. اتشالت، وضفت `break-inside: avoid` على كل بلوك عشان محدش يتقطع نص طباعة الصفحة. دلوقتي أي يوم فيه بلوكات كتير هيطبع كامل (لو محتاج مساحة أكتر من صفحة، هيكمل على الصفحة اللي بعدها بدل ما ينقص).

## 6) الموبايل — اسامي الأيام بتظهر فوق مودال الإعدادات
السبب: `.sch-body` (اللي فيه شريط أيام الأسبوع) كان عليه خاصية قديمة `-webkit-overflow-scrolling: touch`. الخاصية دي معروفة إنها بتخلي سفاري في الآيفون يحط المحتوى القابل للسكرول في طبقة رسم منفصلة، وأحيانًا الطبقة دي بترسم فوق أي مودال ثابت (fixed) بيتفتح بعدها — حتى لو الـ z-index بتاعه أعلى.
اتشالت الخاصية دي (سفاري الحديث مش محتاجها أصلاً)، وكمان زودت z-index بتاع خلفية المودال (الإعدادات + إضافة/تعديل بلوك) لأعلى قيمة وضفتلها `transform: translateZ(0)` عشان تتحط في طبقة رسم منفصلة فوق أي حاجة تانية، حتى لو حصل نفس السلوك في أجهزة تانية.

## فحص جودة الكود
- كل الملفات اتفحصت syntax-wise بـ esbuild (JSX + JS) من غير أي خطأ.
- عملت full bundle check من `SchedulePage.jsx` لكل الملفات المرتبطة بيه (schedule.js, dayAchievements.js, schedulePdf.js...) للتأكد إن كل الـ imports/exports الجديدة (`hasTime`, `importScheduleData`, `exportScheduleData`, `srsBlocksForDate`) متوصلة صح — عدى الـ bundle من غير أي خطأ.
- ماقدرتش أشغّل `npm run build` أو `vitest` فعليًا (مفيش اتصال إنترنت هنا لتحميل node_modules)، فلو عندك وقت شغّل `npm run build` و`npm run test` عندك بعد النسخ للتأكد الكامل.
