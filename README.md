اناااااااااااااااااااااااااااااااااااا بصصصصصصصصصصصصصصصصصصصصصصيححححححححححححححححححححححححححححححححححححححححححححححح يميكولللللللللللللللللللللللللللللللللللللللللللللللللللللللللللللللللللللللل

---

## ملاحظات الأداء (تعديل جديد)

المشروع كان بيحمّل React وBabel من unpkg.com، وبيترجم كل كود JSX (٢٦٠٠ سطر) داخل المتصفح
مباشرة عند كل زيارة — ده أكبر سبب في بطء وثقل الموقع. النهاردة اتعمل الآتي:

1. **تحويل المشروع لـ Vite** بدل تحميل React/Babel من CDN وتفسير الكود live في المتصفح.
   الكود دلوقتي بيتبني (build) مرة واحدة وقت الـ deploy، فبيوصل للمستخدم كملف JS جاهز
   ومضغوط (minified) بدل ما يترجم عندك أنت في كل مرة.
2. فصل الكود لملفات: `src/App.jsx` (المكوّن الرئيسي) و`src/main.jsx` (نقطة التشغيل)
   و`src/index.css` (التنسيقات) بدل ما يكونوا كلهم جوه `index.html` واحد ٢٦٠٠ سطر.
3. شيلنا وزن الخط `500` من روابط Google Fonts لأنه مش مستخدم في الكود خالص (بس ٤٠٠/٦٠٠/٧٠٠
   وAmiri ٤٠٠/٧٠٠) — تقليل حجم الخطوط اللي بتتحمل.
4. إضافة `preconnect` لـ `fonts.gstatic.com` (كان ناقص) عشان تسريع تحميل الخطوط.
5. ملفات الـ API (`api/jsonbin.js`, `api/login.js`) ماتغيرتش في المنطق، اتنقلوا زي ما هما
   (Vercel هيتعرف عليهم تلقائي كـ serverless functions).

### إزاي تشغّل/تنشر المشروع الجديد

```bash
npm install
npm run dev      # للتجربة محليًا
npm run build    # لعمل نسخة إنتاج جاهزة في مجلد dist
```

على Vercel: ارفع المشروع زي ما هو، Vercel هيتعرف إنه مشروع Vite تلقائي (لأن `vite` موجود
في `package.json`) ويعمل build صح، وبرضو هيفعّل `api/jsonbin.js` و`api/login.js` كـ
serverless functions زي الأول بالظبط. متغيرات البيئة (`JSONBIN_BIN_ID`, `JSONBIN_MASTER_KEY`,
متغير `ACCESS_CODE` لم يعد مستخدمًا (تم إلغاء رمز الوصول المشترك — الدخول باليوزرنيم والباسورد فقط).

**تنبيه:** الكود اتفحص وتترجم محليًا بـ esbuild للتأكد إنه سليم، لكن معنديش اتصال إنترنت
في البيئة دي عشان أعمل `npm install` كامل وأجرب `npm run build` فعليًا لحد النهاية. لو حصل
أي خطأ بسيط وقت أول build عندك (على الأغلب مش هيحصل)، ابعتلي رسالة الخطأ وأنا أظبطه فورًا.

---

## مؤقّت المذاكرة (Timer)

من قائمة **المزيد ⋯** → **مؤقّت**.

- عدّ تنازلي أو ساعة توقيت، بدون قيد على المدة.
- تخصيص الخط / الحجم / لون النص / خلفيات جاهزة أو صورة من الجهاز.
- **X أثناء التشغيل:** يصغّر لفقاعة فوق الديكشنري (الديكشنري يرجع يظهر) من غير إيقاف المؤقّت. **X والمؤقّت واقف:** رجوع كامل للديكشنري.
- **موبايل:** الفقاعة جوه التطبيق فقط — المتصفح/النظام **ممنوع** إن موقع يطفو فوق تطبيقات تانية أو بره المتصفح. (قيد نظام، مش نقص في الكود)
- **كمبيوتر (Chrome):** Document Picture-in-Picture = نافذة فوق التطبيقات التانية قدر الإمكان.
- أثناء التشغيل: محاولة إبقاء الشاشة فاتحة (Screen Wake Lock).

---

## تقويم المذاكرة (Study Calendar)

من قائمة **المزيد ⋯** → **التقويم**.

- عرض شهري لأيام المذاكرة (الكلمات اللي اتعلّمتها)، مع كثافة لونية حسب عدد الكلمات في اليوم.
- الضغط على يوم يعرض قائمة الكلمات اللي اتعلّمتها فيه.
- سلسلة الأيام المتتالية (streak) ظاهرة في الأعلى.
- زر **تثبيت**: يصغّر التقويم لودجت عائم فوق الديكشنري (ممكن تسحبه)، ويبقى ظاهر لحد ما تقفله. الحالة بتتسجّل في المتصفح عشان تفضل بعد الريفرش.

---

## قاعدة للتعديلات الجاية (مهم — موبايل أولًا)

أي ميزة أو UI جديد لازم يتصمَّم ويتجرب على **شاشات صغيرة (عرض ≤ 768px)** قبل ما يتعتبر جاهز:

1. **مت تعتمد على `window.open` كنافذة صغيرة على الموبايل** — المتصفحات على التليفون بتفتح تبويب كامل (`about:blank`) مش popup. استخدم overlay / portal داخل الصفحة، أو PiP لو متاح على الديسكتوب فقط.
2. الأزرار والنصوص في الهيدر لازم تتحمل **التفاف (flex-wrap)** ومساحات لمس ≥ ~40px.
3. تجنّب عروض ثابتة بالـ px على عناصر أساسية؛ استخدم `min/max/clamp` و`100dvh` و`env(safe-area-inset-*)` عند الحاجة.
4. اختبر اللمس (سحب، ضغط) مش بس الماوس.
5. لو الميزة بتفتح طبقة فوق التطبيق، تأكد إنها مش بتغطي المحتوى بطريقة تكسر الاستخدام على شاشة ضيقة.


---

## تحسينات أداء إضافية (أغسطس 2026)

- فصل `INFO_SECTIONS` إلى `src/lib/config/infoSections.js`
- فصل دوال النص العربي `stretchArabicText` / `hasArabic` إلى `src/lib/utils/arabicText.js`
- إضافة `React.memo` على `HeaderMenu` و `MainView` لتقليل إعادة الرسم غير الضرورية
- حذف ملف `dev_server.log` من الحزمة

الملفات الكبيرة المتبقية (`HeaderMenu.jsx` ~2k سطر، `App.jsx`، `MainView.jsx`) ما زالت تحتاج تقسيم أعمق للـ modals الداخلية في المستقبل (Settings / Banner / Notifications) لو حابب نكمل في جولة تانية.

### تقسيم إضافي (نفس الجلسة)
- `BannerModal.jsx` — بانر الموقع (أدمن) + كل الـ form state والـ effects
- `NotificationsModal.jsx` — التذكيرات + بث الإشعارات
- `AppearanceModal.jsx` — الشعار / الثيم / الكثافة / الزوايا / الألوان
- `HeaderMenu.jsx` انخفض من ~2200 سطر إلى ~1030 سطر

### تقسيم كامل (جلسة أغسطس 2026)

**HeaderMenu (~2200 → ~770 سطر)**
- `BannerModal.jsx` — بانر الأدمن
- `NotificationsModal.jsx` — تذكيرات + بث
- `AppearanceModal.jsx` — شعار / ثيم / كثافة / ألوان
- `SettingsModal.jsx` — قائمة الإعدادات
- `InfoModal.jsx` — دليل سريع
- `LangModal.jsx` / `AccentModal.jsx` / `DeviceModal.jsx`

**أخرى**
- `src/lib/utils/timerUtils.js` — ثوابت ودوال المؤقّت
- `src/lib/hooks/useAppTheme.js` — ثيم / أكسنت / مقياس الواجهة
- `src/lib/utils/viewPersistence.js` — حفظ حالة الـ overlays
- `src/lib/utils/imageUtils.js` — ضغط صور التسجيل
- `src/lib/config/infoSections.js` — أقسام الدليل
- `src/lib/utils/arabicText.js` — تطويل النص العربي
- `src/styles/{base,layout,components}.css` — تقسيم CSS

`App.jsx` و `MainView.jsx` ما زالوا كبار نسبيًا لأنهم state hubs؛ التقسيم الجوهري للـ UI اتعمل.
